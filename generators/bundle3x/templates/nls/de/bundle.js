define(
    ({
        bundleName: "<%= name %>",
        bundleDescription: "<%= description %>",
        ui: {
            toolTitle: "<%= name %> Werkzeug",
            windowTitle: "<%= name %> Fenster",
        }
    })
);
